name: Show Secret

on:
  push:
    branches:
      - main

jobs:
  reveal-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Output Secret
        run: echo "This is the name of this repository: ${{ github.repository }}

